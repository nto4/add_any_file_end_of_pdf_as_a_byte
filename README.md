```
# add_any_file_end_of_pdf_as_a_byte

# for hide some files u can hide multiple file once
python encode_decode.py -p my_pdf_file.pdf file_to_be_stored.txt


# extractt files
python encode_decode.py -g include_hidden_data.pdf 



usage example:

# encode
python encode_decode.py -p sample.pdf mysecret.txt

# decode
python encode_decode.py -g sample_patched.pdf
```
